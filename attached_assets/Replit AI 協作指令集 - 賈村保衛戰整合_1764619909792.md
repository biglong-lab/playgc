# Replit AI 協作指令集 - 賈村保衛戰整合

本文件包含可直接複製給 Replit AI 的指令,協助您快速完成遊戲整合。

---

## 🎯 指令 1: 建立關卡內容顯示系統

```
我需要建立一個動態的關卡內容顯示系統。

需求:
1. 建立一個 PageRenderer 組件,位於 client/src/components/game/PageRenderer.tsx
2. 根據 page.type 動態渲染不同類型的頁面
3. 支援以下頁面類型:

頁面類型與渲染邏輯:

- story: 故事頁面
  - 顯示 content.text (文字內容)
  - 顯示 content.image_url (圖片)
  - 播放 content.audio_url (音訊,可選)
  - 「下一步」按鈕

- dialogue: 對話頁面
  - 顯示 content.speaker (說話者名稱)
  - 顯示 content.avatar_url (頭像)
  - 顯示 content.text (對話內容)
  - 顯示 content.choices 陣列作為選項按鈕
  - 點擊選項後跳到 next_page

- video: 影片頁面
  - 播放 content.video_url
  - 顯示 content.thumbnail_url 作為封面
  - 顯示播放進度條
  - 「跳過」和「繼續」按鈕

- button: 按鈕頁面
  - 顯示 content.buttons 陣列
  - 每個按鈕包含: text, action, params, image_url
  - 處理不同的 action 類型:
    - grant_item: 給予道具
    - complete_location: 完成關卡
    - unlock_location: 解鎖關卡
    - show_score: 顯示積分
    - show_leaderboard: 顯示排行榜

- text_verify: 文字驗證頁面
  - 顯示 content.question (問題)
  - 輸入框讓玩家輸入答案
  - 比對 content.correct_answer
  - 顯示 content.hint (提示,可選)
  - 限制 content.max_attempts 次嘗試
  - 答對後執行 content.on_success

- choice_verify: 選擇驗證頁面
  - 顯示 content.questions 陣列
  - 每個問題包含: question, options, correct_answer
  - 單選或多選
  - 計算正確率,達到 content.passing_score 才通過
  - 通過後執行 content.on_success

- condition_verify: 條件驗證頁面
  - 檢查 content.conditions 陣列
  - 支援條件類型:
    - has_item: 檢查是否擁有道具
    - visited_location: 檢查是否到訪過地點
    - score_above: 檢查積分是否達標
  - 顯示 content.all_required 決定是否需要全部滿足
  - 顯示 content.success_message 或 content.failure_message

請使用 TypeScript + React + Tailwind CSS 實作。
組件應接收 page 物件和 onComplete 回調函數。
```

---

## 🎯 指令 2: 整合 QR Code 掃描功能

```
我需要在玩家端整合 QR Code 掃描功能。

需求:
1. 安裝 html5-qrcode 套件: npm install html5-qrcode
2. 建立 QRScanner 組件,位於 client/src/components/game/QRScanner.tsx
3. 功能包含:
   - 開啟相機掃描 QR Code
   - 顯示掃描框 (250x250px)
   - 掃描成功後關閉相機
   - 將掃描結果傳給父組件
   - 顯示「關閉」按鈕
   - 處理權限錯誤

4. 在 GamePlay 頁面加入「掃描 QR Code」按鈕
5. 掃描後比對 locations 資料表的 qr_code_data
6. 如果匹配:
   - 顯示「已解鎖關卡」訊息
   - 更新 player_progress
   - 自動開始該關卡
7. 如果不匹配:
   - 顯示「QR Code 無效」錯誤訊息

請使用 TypeScript + React + Tailwind CSS 實作。
確保處理相機權限請求與錯誤情況。
```

---

## 🎯 指令 3: 建立管理後台地點編輯功能

```
我需要在管理後台建立地點管理功能,讓我可以在現場設定 GPS 座標。

需求:
1. 建立 LocationEditor 頁面,位於 client/src/pages/Admin/LocationEditor.tsx
2. 功能包含:

主要功能:
- 顯示所有地點列表 (從 /api/games/1/locations 讀取)
- 每個地點卡片顯示:
  - 順序編號 (order_index)
  - 關卡名稱 (name)
  - 關卡描述 (description)
  - 地點類型 (type)
  - GPS 座標狀態 (已設定/未設定)
  - 觸發半徑 (trigger_radius)
  - 積分獎勵 (points_reward)
  - 「編輯」和「刪除」按鈕

編輯表單:
- 所有欄位可編輯
- GPS 座標輸入框 (latitude, longitude)
- 「使用當前位置」按鈕:
  - 使用 navigator.geolocation.getCurrentPosition()
  - enableHighAccuracy: true
  - 自動填入座標
  - 顯示成功/失敗訊息
- 觸發半徑滑桿 (5-100公尺)
- 地點類型下拉選單
- 必要關卡勾選框
- 「儲存」和「取消」按鈕

API 整合:
- GET /api/games/:gameId/locations - 取得地點列表
- PUT /api/locations/:id - 更新地點
- DELETE /api/locations/:id - 刪除地點

請使用 TypeScript + React + Tailwind CSS + Lucide Icons 實作。
確保 GPS 定位功能在戶外環境正常運作。
```

---

## 🎯 指令 4: 實作關卡觸發檢測系統

```
我需要實作自動的關卡觸發檢測系統。

需求:
1. 在 GamePlay 頁面持續追蹤玩家位置
2. 每 5 秒更新一次位置到後端
3. 檢查玩家是否進入任何關卡的觸發範圍

實作細節:

GPS 追蹤:
- 使用 navigator.geolocation.watchPosition()
- enableHighAccuracy: true
- timeout: 10000
- maximumAge: 0

距離計算:
- 使用 Haversine 公式計算距離
- 比對所有未完成的關卡
- 如果距離 <= trigger_radius,觸發關卡

觸發邏輯:
- 顯示「已到達關卡」通知
- 顯示關卡名稱與描述
- 顯示距離資訊
- 顯示「開始關卡」按鈕
- 或「掃描 QR Code」按鈕 (如果有設定)

位置更新 API:
- POST /api/sessions/:sessionId/player-location
- Body: { latitude, longitude, accuracy, timestamp }

請在 client/src/services/locationService.ts 建立服務層。
請在 client/src/hooks/useLocationTracking.ts 建立自訂 Hook。
使用 TypeScript + React 實作。
```

---

## 🎯 指令 5: 建立遊戲選擇頁面

```
我需要建立一個遊戲選擇頁面,讓玩家可以選擇要玩的遊戲。

需求:
1. 建立 GameSelection 頁面,位於 client/src/pages/Player/GameSelection.tsx
2. 從 /api/games 讀取所有可用遊戲
3. 以卡片形式顯示每個遊戲

卡片內容:
- 封面圖片 (cover_image_url)
- 遊戲名稱 (title)
- 遊戲描述 (description)
- 難度標籤 (difficulty): easy/medium/hard
- 預估時長 (estimated_duration,轉換為小時/分鐘)
- 關卡數量 (從 locations 計算)
- 「開始遊戲」按鈕

點擊「開始遊戲」:
1. POST /api/sessions 建立新場次
   - Body: { game_id, player_id }
2. 取得 session_id
3. 導航到 /game/:sessionId

樣式:
- 使用卡片網格佈局 (grid)
- 響應式設計 (手機 1 列,平板 2 列,桌面 3 列)
- 軍事戰術風格配色
- Hover 效果與動畫

請使用 TypeScript + React + Tailwind CSS 實作。
```

---

## 🎯 指令 6: 優化地圖顯示與導航

```
我需要優化現有的地圖顯示功能,加入更好的導航體驗。

需求:
1. 在 MapView 組件加入以下功能

地圖標記:
- 玩家位置: 藍色圓點,持續更新
- 當前關卡: 紅色標記,閃爍動畫
- 已完成關卡: 綠色標記,打勾圖示
- 未解鎖關卡: 灰色標記,鎖頭圖示
- 觸發範圍: 半透明圓圈

導航資訊面板:
- 顯示到當前關卡的距離
- 顯示方向箭頭 (指向關卡)
- 顯示預估到達時間 (以步行速度 5km/h 計算)
- 顯示「導航」按鈕 (開啟 Google Maps 導航)

地圖控制:
- 「定位」按鈕: 地圖中心移到玩家位置
- 「顯示全部」按鈕: 縮放顯示所有關卡
- 圖層切換: 衛星圖/街道圖

效能優化:
- 使用 React.memo 避免不必要的重渲染
- 使用 useMemo 快取計算結果
- 限制地圖更新頻率 (最多每秒 1 次)

請優化現有的 MapView.tsx 組件。
使用 Leaflet.js + React-Leaflet。
```

---

## 🎯 指令 7: 建立道具系統與背包介面

```
我需要完善道具系統,包含背包介面與道具使用功能。

需求:
1. 建立 Inventory 組件,位於 client/src/components/game/Inventory.tsx
2. 從 /api/sessions/:sessionId/inventory 讀取玩家背包

背包介面:
- 以網格顯示所有道具
- 每個道具卡片顯示:
  - 道具圖片 (image_url)
  - 道具名稱 (name)
  - 道具描述 (description)
  - 稀有度標籤 (rarity): common/uncommon/rare/epic/legendary
  - 數量 (quantity,如果是消耗品)
  - 「使用」按鈕 (如果是消耗品)
  - 「詳細資訊」按鈕

道具分類:
- 全部
- 裝備 (equipment)
- 消耗品 (consumable)
- 徽章 (badge)
- 情報 (intel)
- 特殊 (special)

道具使用:
- 提示卡: 顯示當前關卡的提示
- 時間延長卡: 延長遊戲時間
- 能量補給包: 加速任務完成
- 跳過卡: 跳過當前關卡 (限用 1 次)

API:
- GET /api/sessions/:sessionId/inventory - 取得背包
- POST /api/items/:itemId/use - 使用道具

請使用 TypeScript + React + Tailwind CSS 實作。
加入道具使用動畫效果。
```

---

## 🎯 指令 8: 建立成就系統與解鎖動畫

```
我需要建立成就系統,當玩家達成特定條件時解鎖成就。

需求:
1. 建立 Achievements 頁面,位於 client/src/pages/Player/Achievements.tsx
2. 從 /api/games/:gameId/achievements 讀取所有成就
3. 從 /api/sessions/:sessionId/achievements 讀取玩家已解鎖成就

成就顯示:
- 以卡片網格顯示所有成就
- 已解鎖: 彩色顯示,顯示解鎖時間
- 未解鎖: 灰階顯示,顯示解鎖條件
- 成就類型標籤: location/special/legendary/collection/speed/exploration

成就卡片:
- 成就圖示 (icon_url)
- 成就名稱 (name)
- 成就描述 (description)
- 積分獎勵 (points_reward)
- 解鎖進度條 (如果可追蹤)

解鎖動畫:
- 當成就解鎖時,顯示全螢幕動畫
- 成就圖示從小到大彈出
- 顯示成就名稱與描述
- 顯示獲得積分
- 播放音效
- 3 秒後自動關閉

成就檢查:
- 在適當時機檢查成就條件
- 完成關卡後檢查
- 獲得道具後檢查
- 遊戲結束後檢查

請使用 TypeScript + React + Tailwind CSS + Framer Motion 實作。
```

---

## 🎯 指令 9: 建立排行榜系統

```
我需要優化排行榜系統,加入多種排序方式與篩選功能。

需求:
1. 優化 Leaderboard 頁面,位於 client/src/pages/Player/Leaderboard.tsx
2. 從 /api/leaderboard 讀取排行榜資料

排行榜類型:
- 總積分排行
- 完成時間排行
- 本週排行
- 本月排行
- 全時間排行

顯示內容:
- 排名 (1, 2, 3 顯示獎牌圖示)
- 玩家頭像
- 玩家名稱
- 總積分
- 完成時間
- 完成日期
- 成就數量

篩選功能:
- 遊戲選擇下拉選單
- 時間範圍選擇
- 只顯示好友 (如果有社交功能)

我的排名:
- 在列表中高亮顯示
- 固定在底部顯示 (如果不在前 10 名)

請使用 TypeScript + React + Tailwind CSS 實作。
加入排名變化動畫。
```

---

## 🎯 指令 10: 建立遊戲統計儀表板

```
我需要在管理後台建立遊戲統計儀表板。

需求:
1. 建立 GameAnalytics 頁面,位於 client/src/pages/Admin/GameAnalytics.tsx
2. 從多個 API 端點讀取統計資料

統計卡片:
- 總遊玩次數
- 今日遊玩次數
- 平均完成時間
- 完成率
- 平均積分
- 活躍玩家數

圖表:
1. 遊玩次數趨勢圖 (折線圖)
   - X 軸: 日期
   - Y 軸: 遊玩次數
   - 可選擇時間範圍: 7天/30天/90天

2. 關卡完成率 (長條圖)
   - X 軸: 關卡名稱
   - Y 軸: 完成率 %
   - 顯示每個關卡的完成人數

3. 平均停留時間 (長條圖)
   - X 軸: 關卡名稱
   - Y 軸: 平均停留時間 (分鐘)

4. 道具使用統計 (圓餅圖)
   - 顯示各道具的使用次數佔比

5. 積分分佈 (直方圖)
   - X 軸: 積分區間
   - Y 軸: 玩家數量

API:
- GET /api/analytics/overview - 總覽統計
- GET /api/analytics/trends - 趨勢資料
- GET /api/analytics/locations - 關卡統計
- GET /api/analytics/items - 道具統計

請使用 TypeScript + React + Tailwind CSS + Recharts 實作。
```

---

## 📋 使用方式

1. **複製指令**: 選擇您需要的功能,複製對應的指令
2. **貼到 Replit AI**: 在 Replit 專案中開啟 AI 助手,貼上指令
3. **確認需求**: Replit AI 可能會詢問細節,請根據實際情況回答
4. **測試功能**: AI 完成後,測試功能是否正常運作
5. **調整優化**: 如有需要,請 AI 進行調整

---

## 💡 提示

- 一次只執行一個指令,確保每個功能都正常運作後再繼續
- 如果 AI 產生的程式碼有錯誤,可以要求它修正
- 可以要求 AI 加入額外的功能或調整樣式
- 建議按照指令順序執行,因為後面的功能可能依賴前面的組件

---

## 🔗 相關文件

- `REPLIT_INTEGRATION_GUIDE.md` - 完整整合指南
- `GAME_DESIGN.md` - 遊戲設計文件
- `GAME_DATA.sql` - 遊戲資料

祝您開發順利! 🚀
