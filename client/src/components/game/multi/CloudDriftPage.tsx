import { CloudDrift } from "./CloudDrift";

interface Props {
  gameId: string;
  sessionId: string;
  pageId: string;
  isTeamLead?: boolean;
  config?: Record<string, unknown>;
}

export default function CloudDriftPage({ gameId, sessionId, pageId, isTeamLead, config }: Props) {
  return (
    <CloudDrift
      gameId={gameId}
      sessionId={sessionId}
      pageId={pageId}
      isTeamLead={isTeamLead}
      config={config as { title?: string; prompt?: string } | undefined}
    />
  );
}
