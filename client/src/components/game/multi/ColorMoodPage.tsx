import { ColorMood } from "./ColorMood";

interface Props {
  gameId: string;
  sessionId: string;
  pageId: string;
  isTeamLead?: boolean;
  config?: Record<string, unknown>;
}

export default function ColorMoodPage({ gameId, sessionId, pageId, isTeamLead, config }: Props) {
  return (
    <ColorMood
      gameId={gameId}
      sessionId={sessionId}
      pageId={pageId}
      isTeamLead={isTeamLead}
      config={config as { title?: string; prompt?: string } | undefined}
    />
  );
}
