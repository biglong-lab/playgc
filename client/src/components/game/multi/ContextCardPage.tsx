import { ContextCard } from "./ContextCard";

interface Props {
  gameId: string;
  sessionId: string;
  pageId: string;
  isTeamLead?: boolean;
  config?: Record<string, unknown>;
}

export default function ContextCardPage({ gameId, sessionId, pageId, isTeamLead, config }: Props) {
  return (
    <ContextCard
      gameId={gameId}
      sessionId={sessionId}
      pageId={pageId}
      isTeamLead={isTeamLead}
      config={config as { title?: string; prompt?: string } | undefined}
    />
  );
}
