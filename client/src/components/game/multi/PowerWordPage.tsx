import { PowerWord } from "./PowerWord";

interface Props {
  gameId: string;
  sessionId: string;
  pageId: string;
  isTeamLead?: boolean;
  config?: Record<string, unknown>;
}

export default function PowerWordPage({ gameId, sessionId, pageId, isTeamLead, config }: Props) {
  return (
    <PowerWord
      gameId={gameId}
      sessionId={sessionId}
      pageId={pageId}
      isTeamLead={isTeamLead}
      config={config as { title?: string; prompt?: string } | undefined}
    />
  );
}
