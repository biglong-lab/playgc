import { SandTimer } from "./SandTimer";

interface Props {
  gameId: string;
  sessionId: string;
  pageId: string;
  isTeamLead?: boolean;
  config?: Record<string, unknown>;
}

export default function SandTimerPage({ gameId, sessionId, pageId, isTeamLead, config }: Props) {
  return (
    <SandTimer
      gameId={gameId}
      sessionId={sessionId}
      pageId={pageId}
      isTeamLead={isTeamLead}
      config={config as { title?: string; prompt?: string } | undefined}
    />
  );
}
