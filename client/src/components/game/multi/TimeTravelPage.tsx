import { TimeTravel } from "./TimeTravel";

interface Props {
  gameId: string;
  sessionId: string;
  pageId: string;
  isTeamLead?: boolean;
  config?: Record<string, unknown>;
}

export default function TimeTravelPage({ gameId, sessionId, pageId, isTeamLead, config }: Props) {
  return (
    <TimeTravel
      gameId={gameId}
      sessionId={sessionId}
      pageId={pageId}
      isTeamLead={isTeamLead}
      config={config as { title?: string; prompt?: string } | undefined}
    />
  );
}
