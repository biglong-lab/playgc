# Logs — 2026-05-04

> Append-only、不刪不改舊紀錄
> 紀錄格式六欄（見 [PROTOCOL.md](../PROTOCOL.md#step-3-完成後--append-紀錄)）

---

### 2026-05-04 [Claude]
- **動作**：🛡️ /admin/manual 兩個問題修補（使用者回報）
  - 問題 1：手冊內相對連結 `[01-host](01-host-components.md)` 在 /admin/manual 路徑下被瀏覽器解析為 `/admin/01-host-components.md` → 404
  - 問題 2：repo 為 PRIVATE、不該外連 github.com（洩露結構 + 私有資料）
- **檢查**：TS 零錯誤 ✅ / smoke 51/51 ✅ / 部署後 HTML 不含 github.com 字樣 ✅
- **結果**：commit `1b931f5a` + `2a1f1657` push main + 部署生效
  - markdown-mini.ts 重寫 link 解析：純 .md filename → 內部 tab 切換 / `../*.md` → plain text / github.com 私有 repo URL → 強制 plain text
  - ManualPage.tsx：6 個 GitHub 外連 → 「延伸文件清單」純展示（無 url）/ 載入失敗 fallback 改「聯絡工程團隊」/ Footer 移除 GitHub 編輯提示
  - 新增 onClick 攔截 a[data-manual-file]：點擊內部連結切 tab + scroll to top
- **判斷**：資安改善（不洩露 repo 結構）+ 體驗改善（手冊內 tab 切換流暢、不再 404）
- **檔案**：2 修改（markdown-mini.ts + ManualPage.tsx）
- **任務 ID**：Manual-Security-Fix

### 2026-05-04 [Claude]
- **動作**：🐛 修「創建隊伍」按鈕 JSON circular structure bug（使用者實測截圖回報）
- **檢查**：TS 零錯誤 ✅ / useTeamLobby.test.ts 21/21 ✅ / smoke 51/51 ✅
- **結果**：commit `dae11bcb` push main + 部署生效
- **根因**：
  - LobbyViews.tsx:388 `<Button onClick={onCreate}>` 直接綁
  - React 把 SyntheticEvent 當第一個參數傳 → handleCreateTeam(squadId) 中 squadId 變 React event 物件
  - createTeamMutation.mutate({ name, squadId: ReactEvent }) → fetch body JSON.stringify(ReactEvent) → circular 失敗
- **修法（兩道防線）**：
  1. LobbyViews.tsx:388 — `onClick={onCreate}` → `onClick={() => onCreate()}`
  2. useTeamLobby.ts:391 — handleCreateTeam 加 type guard（`typeof squadId === "string"` 才用、否則 undefined）
- **判斷**：
  - 線上問題、玩家無法組隊、優先修
  - 多人活動立即可恢復使用
  - type guard 同時防未來其他呼叫處再踩同雷
- **建議下一步**：
  - 使用者重測組隊 → 創建隊伍應該成功
  - sweep 其他 mutation handler 是否有類似 onClick={fn} fn 接參數 的 pattern（暫無發現）
- **檔案**：2 修改（LobbyViews.tsx + useTeamLobby.ts、共 4 行 +）
- **任務 ID**：Team-Create-JSON-Fix
