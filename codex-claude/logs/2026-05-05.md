# Logs — 2026-05-05

> Append-only、不刪不改舊紀錄
> 紀錄格式六欄（見 [PROTOCOL.md](../PROTOCOL.md#step-3-完成後--append-紀錄)）

---

### 2026-05-05 [Claude]
- **動作**：🐛 修「用既有隊伍出戰」400 + 「建新隊伍」誤帶 squadId 403 兩個 bug（使用者實測截圖回報）
- **檢查**：TS 零錯誤 ✅ / smoke 51/51 ✅ / useTeamLobby 21/21 + teams 35/35 + KeepSquadDialog 11/11 ✅
- **結果**：commit `15f19728` push main + 部署生效
- **根因 / 修法**：
  - **Bug A（400 too_small）**：handleCreateTeam 送 name: ""、後端 schema `z.string().min(1).optional()` 拒絕空字串
    - 修：server schema 加 `.transform()` 把 "" 轉 undefined + client trim 後若空就不送 name 欄位
  - **Bug B（403 不是成員）**：handleCreateTeam fallback `getLastUsedSquadId()` 從 localStorage 拉到上次的 squadId、但這次想建新 team
    - 修：移除 fallback 邏輯（PR4 舊設計、新 mySquads 列表 UI 露出後已多餘）
    - 顯式 squadId-only：點「用 X」帶 squadId、點「建另一隊」不帶
- **判斷**：兩個雷都跟「自動聰明」邏輯有關、明確的「使用者意圖 → 程式 1:1 對應」更可靠
- **檔案**：2 修改（teams.ts schema + useTeamLobby.ts handleCreateTeam）
- **任務 ID**：Team-Create-Both-Bug-Fix

### 2026-05-05 [Claude]
- **動作**：🛡️ 解散隊伍防誤刪（重新輸入隊名才能解散）+ 修「解散後隊伍仍顯示在列表」bug
- **檢查**：TS 零錯誤 ✅ / smoke 51/51 ✅ / DisbandSquadDialog 11/11 tests 全綠
- **結果**：commit `05751302` push main + 部署生效
- **根因 / 修法**：
  - **Bug A（解散後仍顯示）**：SquadSettings 解散完只 `navigate("/me")`、沒 invalidateQueries(["/api/me/squads"])
    - 修：useMutation onSuccess invalidate /api/me/squads + /api/squads/:id、再 navigate("/me/squads")
    - 後端原本 soft delete 是對的（squad.status="dissolved" + squad_members.leftAt=now）、純前端快取問題
  - **Bug B（window.confirm 太鬆）**：原使用瀏覽器原生 alert、容易誤按 OK
    - 修：新建 DisbandSquadDialog 元件（96 行）、必須輸入 `「公司勇者隊」` 確認解散
    - 4 個明確警告（成員退出 / 180 天鎖名 / 戰績凍結 / 無法復原）
    - typed.trim() === squadName 時確認按鈕才啟用
- **判斷**：高破壞性動作（刪除 / 解散）必須配「需重輸名稱」防誤刪、`window.confirm` 不再使用
- **檔案**：3 個（1 新增 DisbandSquadDialog.tsx + 1 新增 test + 1 修改 SquadSettings.tsx）
- **任務 ID**：Squad-Disband-Antifoot

### 2026-05-05 [Claude]
- **動作**：🐛 修 PWA「立即更新」按一次就好 + 即時 visual feedback
- **檢查**：TS 零錯誤 ✅ / smoke 51/51 ✅
- **結果**：commit `efe304b5` push main + 部署生效
- **根因**：handleUpdate 沒 disable 按鈕、使用者連點 N 次觸發 race
  - 每次 reg.update() 1-3 秒 + setTimeout 2000ms reload 排隊
  - 多個 unregister 競爭、SW state 混亂
  - 沒 visual feedback、使用者「按了沒看到反應」 → 重複按
- **修法（兩道防線 + 即時 feedback）**：
  1. updatingRef 即時 lock + isUpdating state（雙重保險）
  2. 按鈕 disabled + Loader2 spinner + 「更新中...」標題
  3. 縮短 fallback timeout (2000ms → 800ms)
  4. 加 5 秒 hardReloadTimer 終極保險（防 SW message 丟失）
- **判斷**：所有「異步動作 + 觸發後重 load」UI 都該有 lock + 即時 feedback、避免使用者「以為沒反應」
- **檔案**：1 修改（AppUpdateChecker.tsx、+54 -27 行）
- **任務 ID**：PWA-Update-Click-Once

### 2026-05-05 [Claude]
- **動作**：🎯 組隊系統「先玩、想留再留」核心定位確認 + 全套 P0+P1+P2 實作 + 部署
- **檢查**：TS 零錯誤 ✅ / smoke 51/51 ✅ / 完整 test:run 161/2261 全綠
- **結果**：commit `9f2acdb2` + 紀錄 commit `c3025bc2` push main + 部署生效
- **根因（使用者實測 + 設計討論）**：
  - 使用者實測：「已創建隊伍、退出遊戲、再回又顯示要重新創建」+「我的隊伍只看到水彈」
  - 根因 1（後端 ghost-lobby 過激）：`server/routes/teams.ts:383-384` `team.status='playing' && !activeSessionId` 直接回 null
  - 根因 2（前端 PR4 UI 沒做完）：ADR-0003 已知限制 #2「LobbyViews UI 還沒露出『以隊伍出戰』按鈕」
- **設計確認（與使用者多輪對話、最終共識）**：
  - **核心理念**：「Squad 不是預設條件、是『玩得不錯想留下』的結果」
  - 進場前**不需要**先建 Squad、玩完才問「保留？」
  - Squad 規模不限人數、出戰人數依遊戲 min/maxTeamPlayers
  - 一玩家最多 5 個 active Squad（軟上限）
  - ADR-0003 雙層架構不變、只補前端 UX
- **實作（11 檔 / 751 行新增）**：
  - **P0**：移除 ghost-lobby 過激 + 既有 reconnecting flash 自動 work
  - **P1-3**：LobbyViews CreateTeamForm 三狀態 UI（沒/1+/多 Squad）
  - **P1-4**：useTeamLobby 加 mySquads query
  - **P1-5**：handleCreateTeam 帶 squadId
  - **P1-6+7**：POST /api/teams/:id/promote-to-squad + KeepSquadDialog 元件
    - 含 5 個 Squad 軟上限檢查（409）
  - **P1-8**：MySquads 文案優化（解釋「怎麼來的 / 有什麼用 / 朋友邀你」）
  - **TeamLobbyView**：加中斷恢復 banner + 升級 CTA + 已升級徽章
  - **P2**：docs/changes/2026-05-04-team-flow-redesign.md（200+ 行）+ docs/manual/02-multi-components.md 更新
  - **測試**：KeepSquadDialog.test.tsx 11 tests + teams.test.ts 修對齊新行為
- **判斷**：
  - 補完 ADR-0003 PR4 前端 UI、不推翻雙層架構
  - 使用者體驗 = 一條路（先玩、想留再留）、後端架構 = 既有 Squad/Team/bridge
  - 一次性玩家無感、固定團體自然沉澱
- **建議下一步**：24h 觀察「保留為長期隊伍」CTA 點擊率 + 中斷恢復 banner 觸發率
- **檔案**：11 個（3 新增：promote endpoint + KeepSquadDialog + test、8 修改）
- **任務 ID**：Team-Flow-Redesign-Complete-Deploy

### 2026-05-05 [Claude]
- **動作**：🎯 修 3 個多人遊戲問題（PhotoTeamFlow + ChoiceVerifyRace 全套）+ 部署
- **檢查**：TS 零錯誤 ✅ / smoke 51/51 ✅ / 完整 test:run 163/2287 全綠（+2 檔 +26 tests）
- **結果**：commit `1121e2b3` push main + 部署生效
- **使用者回報 3 個問題**：
  1. 團體拍照「合成團體照中」卡住
  2. 拍照選 N 人但每人拍 N 張（race bug）
  3. 多人搶答（金門腦筋急轉彎）題目不同步、拿不到積分、倒數時間需可設定
- **使用者要求對齊**：預設 20 秒、admin 可設、答完留在最後畫面繼續倒數、時間到一起進下一題、確保大家同一時間看到題目
- **修法**：
  - **問題 1（合成卡住）**：30s timeout 仰賴 AbortController 不夠、Cloudinary hang 不 abort
    - 修：獨立 setTimeout 30s hard fallback + 倒數秒數 UI（「⏱ X 秒後自動使用首張」）
  - **問題 2（race bug）**：useEffect 監聽 camera.capturedImage 有 race
    - 修：改為 imperative（PhotoPreview onSubmit={handleConfirmShot}）+ confirmingRef 即時 lock
  - **問題 3（題目不同步）**：徹底重設計 — server 統一推進
    - 新增 server/lib/race-state.ts (200 行) — in-memory race state per (sessionId+teamId+pageId)
    - 新增 4 個 WS event：race_init / race_state / race_question_advanced / race_complete
    - 重寫 race_answer：server 端記分數 + broadcast
    - server timer 統一推進（依 secondsPerQuestion）、軟邊界 5-120 秒
    - 預設 questionTimeLimit 30 → 20
    - admin game-editor ChoiceVerifyEditor 加「每題秒數」input UI
    - schema ChoiceVerifyConfig 加 questionTimeLimit field
    - client ChoiceVerifyRacePage 完整重寫 server-driven
    - client ChoiceVerifyRace 元件加 server props（向後相容、未提供時走 fallback local mode）
- **判斷**：
  - 使用者要求 6 點對齊全達成 ✅
  - 既有 25 個 ChoiceVerifyRace 測試全保留（向後相容）
  - 新增 race-state.test.ts 15 tests 涵蓋核心邏輯
- **建議下一步**：24h 觀察 race endpoint 使用率、Cloudinary 合成平均耗時、30s timeout 觸發率
- **檔案**：10 個（2 新增：race-state.ts + race-state.test.ts、8 修改）/ +852 -87 行
- **任務 ID**：Multi-3-Problems-Fix-Deploy

### 2026-05-05 [Claude]
- **動作**：✅ 完整 e2e 檢測 — 確保功能完備、所有端點前後端正常
- **檢查**：
  - TypeScript 零錯誤 ✅
  - smoke test 51/51 ✅
  - 完整 test:run **163 檔 / 2287 tests** 全綠 ✅
  - 6 個重點測試檔分項統計：race-state 15 / ChoiceVerifyRace 25 / teams 35 / useTeamLobby 21 / KeepSquadDialog 11 / DisbandSquadDialog 11
  - 10 個生產業務頁全 200（/, /pitch, /find-scenario, /template-market, /showcase, /pricing, /api-docs, /faq, /roi, /admin/manual）
  - 8 個 admin/me/squad SPA 路由全 200
  - 6 個認證保護端點全 401（promote-to-squad, teams/teams, my-team, me/squads, player-photo, composite-photo）
  - WebSocket /ws upgrade 端點可達
  - 6 個手冊靜態檔全 200（README + 01-05）
  - 部署同步：GitHub main HEAD = Production HEAD = `1121e2b3` ✅
  - 生產資源：CPU 0.16% / Memory 25.40% (390MiB/1.5GB) / PIDS 55
- **結果**：全套通過、無遺漏端點、無 regression
- **判斷**：本日 sprint 涵蓋 4 個主要修補（PWA 更新、解散防誤刪、組隊「先玩想留再留」、多人 3 問題）+ 1 個檢測、共 5 個 commit、累計 ~2400 行新增、零事故部署
- **建議下一步**：使用者實測各情境（拍照、搶答、組隊）→ 24h 觀察期 → 累積 telemetry 後再排下個 sprint
- **檔案**：（純驗證、無 commit）
- **任務 ID**：Full-E2E-Verification

---

## 📊 2026-05-05 全日總結

| 維度 | 數量 / 狀態 |
|------|-----------|
| **commits** | 5 個（efe304b5 / 05751302 / 15f19728 / 9f2acdb2 + c3025bc2 / 1121e2b3）|
| **新檔案** | 6 個（DisbandSquadDialog + test、KeepSquadDialog + test、race-state + test）|
| **修改檔案** | 17 個（client / server / shared schema / docs）|
| **新增行數** | ~2,400 |
| **新增測試** | +37 tests（11 + 11 + 15）|
| **完整 test:run** | 163 檔 / 2287 tests 全綠 |
| **smoke test** | 51/51 |
| **生產 e2e** | 全綠（10 業務頁 + 8 admin + 6 API + 6 manual + WS）|
| **部署事故** | 0 |
| **使用者實測 bug 修補** | 5 個（PWA 更新 / 解散殘留 / 創建隊伍 / 退出回不去 / 拍照合成 / 拍照 race / 題目不同步）|
| **新功能** | KeepSquadDialog + DisbandSquadDialog + race-state + admin 每題秒數 UI |

**核心成就**：
1. 🐛 5 個使用者實測 bug 全修
2. 🎯 組隊系統 ADR-0003 PR4 前端 UI 補完 + 「先玩、想留再留」核心理念落實
3. 🏃 多人搶答 server 統一推進、確保公平競爭
4. 📸 拍照流程穩定（imperative + hard timeout）
5. 🛡️ 解散防誤刪保護 + PWA 更新 UX 改善
6. ✅ 完整 e2e 全綠、生產零事故

**未做（下次 sprint 候選）**：
- 24h 觀察 race endpoint 採用率 + KeepSquadDialog 點擊率
- ShowcaseHub 補新元件 multi 軸線 demo
- 真實活動驗證（找 1-2 個情境真實場域跑）
- 多語系 / AI 內容生成擴充
