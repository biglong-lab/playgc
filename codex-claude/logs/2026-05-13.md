# 2026-05-13 工作紀錄

> 角色：[Claude]
> Session：碎片切割完整實作 + 業主回報 7 項 P0/P1/P2 修補 + PWA 三項升級

---

## 🎯 本日里程碑

| commit | 主題 |
|--------|------|
| `415645a2` | 業主回報 P0 三項 — 進行中按鈕 / 卡片區域 / 預覽拍照 400 |
| `31d0453c` | P1 + P2-6 — 右上按鈕重疊 / dropdown 中文直式 / 編輯道具整合 |
| `c8296141` | P2-5 碎片圖切割完整實作（admin + 玩家端 + schema）|
| `572ff8b3` | 碎片整體性 5 項修補（A image 強制 all_collected / B 圖載失敗 fallback / C image 簡化 UI / D 數量建議 / E 切換保留）|
| `ed51505d` | React #310 修補（GamePreview hooks order）— 30 次 ErrorBoundary 根因 |
| `adc26318` | PWA 三項：Lighthouse CI / iOS Splash × 12 解析度 / Background Sync |

---

## 1. P0 三項業主回報修補（commit `415645a2`）

**業主原話**：
- 「遊戲保留進度功能失效、無繼續遊戲按鍵」
- 「按再玩一次以外的區域直接跳到任務完成」
- 「後台預覽模式、會有部分元件出錯無法下一步（拍照失敗 400 缺 Session ID）」

**修補**：
- `Home.tsx` gameStatusMap 邏輯 — 24h 內 playing 優先（不被舊 completed 蓋掉）
- 已完成卡片 → 移除整張 onClick + cursor-default、只「再玩一次」按鈕觸發
- `PhotoBurstFlow` 加 `usePreview()`、preview 模式跳過真上傳

## 2. P1 / P2-6 業主回報修補（commit `31d0453c`）

- **右上角按鈕重疊**：Landing.tsx LanguageSwitcher 移到 `right-32` 避開 FloatingFontScale
- **道具 dropdown 中文直式**：ItemSelect 加 `whitespace-nowrap + truncate + flex-shrink-0`
- **編輯道具對話框整合**：ItemsEditor 簡版升級到完整版（slug + icon picker × 11 + effect type/value）

## 3. P2-5 碎片圖切割完整實作（commit `c8296141`）

**設計**：CSS `background-position + background-size` 動態切割（不真切、不上傳 N 張）

**Admin 端**（`ConditionalVerifyEditor`）：
- `fragmentSource` radio（text / image）
- image 模式 MediaUploadButton 上傳 + 即時 grid 預覽
- `calcFragmentGrid()` 自動算 cols × rows（2-10）

**玩家端**（`ConditionalVerifyPage`）：
- 偵測 fragmentSource = "image" → 用 bg-position 顯示切片
- 未收集 grayscale + Lock icon、收集後彩色 + border-primary
- 右下角碎片序號 badge

**Schema**：`ConditionalVerifyConfig` 加 `fragmentSource` + `fragmentImageUrl`

## 4. 碎片整體性 5 項修補（commit `572ff8b3`）

| # | 問題 | 修法 |
|---|------|------|
| A | 圖片模式仍顯示「輸入密碼」UI | 強制 verificationMode = 'all_collected' |
| B | 圖片載入失敗無 fallback | `new Image()` 預載偵測 + imageLoadFailed state → fallback 文字模式 |
| C | Admin image 模式仍顯示無意義的 value Input | `fragmentSource === "image"` 時隱藏 value Input |
| D | image 模式無建議區間（10 塊太細）| 加「建議 4-9 塊」提示 + 「拼齊自動通過」說明 |
| E | text ↔ image 切換時 sourceItemId 保留 | 已 OK（generateFragments 既有機制） |

## 5. React #310 修補（commit `ed51505d`）

**根因**：`GamePreview.tsx` 違反 React Hooks Rules — `handlePreviewComplete` / `variables` / `handleVariableUpdate` 3 個 hooks 在 early return 後面

**證據**：撈 error_logs 7 天 ErrorBoundary 30 次、其中 27 次都是 React #310、stack trace 指向 `GamePreview.js:1:5683` 的 useCallback

**修法**：把 3 個 hooks 移到 early return 之前

## 6. PWA 三項升級（commit `adc26318`）

**A. Lighthouse CI 自動化**：
- `.github/workflows/lighthouse.yml` — PR / 每週 / manual
- `.github/lighthouse/lighthouserc.json` — mobile profile、Perf 70+ / A11y 90+ / BP 85+ / SEO 90+
- 跑 4 個生產 URL：/, /f/JIACHUN/home, /pitch, /showcase
- PR 自動 comment 分數

**B. iOS PWA Splash × 12 解析度**：
- `scripts/generate-splash-screens.mjs` 用 sharp 動態合成
- 從 `pwa-512.png` + theme color #111827 產生 12 張 splash
- 覆蓋 iPhone 8 / 11 / Xs / 13 / 14 Pro / 14 Plus / 14 Pro Max + iPad mini / Pro 11 / Pro 12.9
- `index.html` 加 apple-touch-startup-image meta（依 device-width/height/dpr media query）

**C. Background Sync（金門場域訊號救命）**：
- `vite.config.ts` workbox.runtimeCaching 加 2 條：
  - `PATCH /api/sessions/:id/progress` → BackgroundSync 24h queue
  - `POST /api/cloudinary/(player-photo|burst-frame|composite-photo)` → 同
- 即使分頁關了 SW 仍會自動 retry
- 玩家在訊號爛地方寫進度 / 拍照 → 回線自動補

---

## 📊 本日總結

| 維度 | 數量 |
|------|------|
| 實質 commits | 6 個（不含 ~10 個 auto chore）|
| 業主回報問題 | 7 項全處理（6 修 + 1 可行性報告先實作 → 完整實作）|
| 新增檔案 | 15 個（CI workflow / splash 12 張 / 文件 4 份）|
| 修補錯誤紀錄根因 | ErrorBoundary 30 次 → 修了 GamePreview 觸發的 27 次 |
| 部署事故 | 0 |

## 後續（5/14 接續）

- 業主回報「碎片預設數量變為 0、無法設定」→ 5/14 排查
- 5/14 找到 stale closure bug、修補 batch updateFields
- 詳見 [2026-05-14.md](2026-05-14.md)
