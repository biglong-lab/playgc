# 2026-05-19 協作日誌

## [Claude 02:30] POS 全部找不到 — 根因修正 + 強制核銷 + 改梯次

### 業主回報
- iPhone 掃 QR：「找不到 預約：BK_fji_PhaDmVSM... (可能不是本場域的、或編號錯誤)」
- 手動輸入 ADGF3Z：同錯誤
- 業主推測時段問題、要求設計「未到時段提醒 / 強制核銷 / 改梯次」流程

### 根因（資料模型不一致）
| 表 | field_id 存什麼 |
|----|---------------|
| `bookings` | 場域代碼（如 `JIACHUN`）|
| `admin_accounts` | 場域 UUID（如 `72cc204d-...`）|
| `resolveFieldId(req)` | 回 UUID |
| 結果 | `WHERE bookings.field_id = UUID` → 永遠不 match |

驗證：生產 DB 查 `ADGF3Z` 確實存在、field_id = `'JIACHUN'`、9 筆全用代碼。

### 修正
1. 新增 `resolveFieldScope(req)`：lookup fields table 同時拿 UUID + code
2. 所有 POS query 改用 `inArray(bookings.fieldId, scope.identifiers)`
3. 覆蓋 7 個 endpoint：dashboard / checkin / check-in / no-show / checkout / voucher/redeem / summary

### 額外 UX 強化（業主想要的「更好的流程」）
1. `/api/pos/checkin` 改為「先全域找、再判場域」、回 5 種狀態：
   - `not_found` — 真不存在
   - `wrong_field` 403 — 此預約屬於 XXX、不是本場
   - `too_early` — 早 N 分鐘到（issue, 200）
   - `too_late` — 晚 N 分鐘（issue, 200）
   - `cancelled` / `no_show` — 已取消（issue, 200）
2. `/api/pos/bookings/:id/check-in` 加 `force=true` 參數：
   - 忽略狀態 + 時段限制
   - 自動將 cancelled / no_show 重啟為 confirmed
   - 寫入 adminNote
3. 新增 `/api/pos/bookings/:id/reschedule`：
   - 業主可現場改梯次（客人提前到場、現場有位置）
   - 不檢查容量（業主已現場判斷）
   - 寫入 adminNote 留軌跡
4. 前端拆出 `PosScanResultCard.tsx`：
   - 五種狀態各自顯示對應 banner（紅 / 琥珀色）
   - 「✅ 強制核銷」按鈕 + confirm 對話框
   - 「📅 改梯次」按鈕 → 開啟 datetime-local 對話框
   - 場域錯誤 toast 改顯「不在本場域」標題

### 部署
- commit `63e94805` push → ssh + docker compose up -d --build
- 生產 `/pos/scan` 200 OK

### 檔案
- `server/routes/pos.ts` (+228 -147) — fieldScope + 5 種狀態 + force + reschedule
- `client/src/pages/pos/PosScan.tsx` (-90 行) — 抽 ResultCard、error toast 分流
- `client/src/pages/pos/PosScanResultCard.tsx` (+379 行新檔)

### 業主測試引導
1. 開 https://game.homi.cc/pos/scan
2. 手動輸入 `ADGF3Z` 應該成功（不再「找不到」）
3. 由於預約是 5/21（還沒到）→ 應顯示「早 X 分鐘」橘色 banner + 「強制核銷」/「改梯次」按鈕
4. 點「強制核銷」→ 確認對話框 → 完成報到
5. 或點「改梯次」→ 改成「現在」→ 確認 → 完成報到
