# 2026-07-09 工作紀錄

> append-only、不刪不改舊紀錄

---

## [Claude] 07-08 深夜 ~ 07-09 — CHITO 11 項總掃 + 全站優化 4 批 + 部署

### 第一部分：CHITO 11 項 bug 修復（07-08、commit `3d9d55ae` 等 3 個）

**多人 4 bug 同一條根因鏈一次修**：
1. `#8687281e` 投票 1/3 跳頁：完成判定改 **server 權威**（新 `lib/team-vote-eval.ts`）；client 只信 serverComplete（cast 回應/WS/10s polling 三路）；GET /votes 補回 completed；成員離開自動重算投票並廣播 `vote_completed`
2. `#0e0f5f17` 卡「同步隊伍進度中」：**真根因 = leader-decide「先繼續」不檢查目標已重連**、在線玩家被踢成 403 死鎖 → 加防呆回 already_back；7 個多人頁 my-team 加 10s refetch
3. `#ec3f612b` 退出重進無法連線：新增 `GET rejoinable-team` + `POST rejoin` 端點、`TeamRequiredFallback` 共用元件（重新連線原隊伍）、GamePlay 帶 ?session= 自動 rejoin
4. `#e2f14e8b` 卡舊隊伍：大廳「歡迎回來」1 秒強制跳轉 → 3 秒倒數 + 立即繼續/離開隊伍按鈕

**單機 5 修 + 2 開關**：
- `#f095652b` 進度保留（第9修）：solo playing session 唯一化（POST /sessions 自動放棄舊 playing）+ 揀選規則對齊 Home 24h + resetAndCreateNew 雙重 mutate 修復
- `#c1149bc8` 道具給分（第7修）：真源頭 = getDefaultConfig 建頁預設 10~50 分悄悄寫入 DB → 全改 0；編輯器顯示 fallback 對齊
- `#c92e32dc` GPS 反向（第9修）：真根因 = Android 用 `deviceorientation`（alpha 非指北）→ 改 `deviceorientationabsolute` + 螢幕方向補償 + gpsdebug 面板 abs/rel 標記
- `#4e88bd7d` BGM 不停：fade 世代控制 + 停止斷根（清 src/wantPlay）+ Home 掛載保險
- `#1bc34792` AR 錄影 WEBP 靜態：ImageDecoder 解幀逐幀合成（iOS fallback 靜態）
- `#93c7a2ca` 通關畫面星星/分數開關（games 加 2 欄 ADD COLUMN）
- `#7b1a6e7d` 按鈕選項得分顯示開關（ButtonConfig.showOptionRewards）

11 筆已 POST 寫回 CHITO（全轉 testing）；多人 4 筆需 3 台真機複測。

### 第二部分：全站優化盤點 + 4 批執行（07-09、commit `58889d1d`）

盤點方法：即時健康數據 + 2 個 Explore agent（架構效能/安全維運）。

- **P0 安全**：修 2 個 IDOR（GET votes/score-history 任何登入者可讀他隊）→ `lib/team-membership.ts` 統一 requireTeamMember；aiLimiter 掛上 4 個 AI 端點（原從未使用）；新增 teamActionLimiter/sessionCreateLimiter
- **P1 穩定**：WS 4 個 Map（teamStateCache 等）閒置 30min 回收；PATCH /progress + chat 補 zod；admin-multi-sessions N+1（S×T×4→4 次）+ admin-rescue（P×2→2 次）
- **P2 效能**：主 bundle 647→622KB（qrcode 出 Home 首屏鏈改動態載入）；58 處 refetchInterval 審查=全合理不動；livekit 確認已自動分包
- **P3 健康**：抽 `useMyTeam` hook（7 容器 -47 行、消除複製漂移）；GamePlay 992→810 行（隊伍同步抽 `useTeamPlaySync` 289 行）；**websocket.ts/Home.tsx 拆分延後下批**

### 部署（07-09、業主口頭核可）

- `npm run deploy` 全綠：commit `58889d1d`、bundle `index-Buebtw9F.js`、/api/version=58889d1d
- 生產 DB 加 `show_completion_stars`/`show_completion_score`（ADD COLUMN IF NOT EXISTS）
- 驗證：首頁 200、rejoinable-team 端點 401（存在）、IDOR 端點 401、安全 headers 齊、舊資料完整（games 42/sessions 909/teams 96/users 182）
- CHANGELOG 標記上線（commit `fbd93dd7` 已 push）

### 系統健康快照

- tsc 0 錯誤、vitest **223 檔 3234 測試全綠**（新增 ~59 個測試）、build 成功
- npm audit：9（0 high、8 moderate、1 low，需 breaking 升級）
- >800 行違規：25 → 24 檔（GamePlay 已解）

### 下一步

1. ⏳ CHITO 11 筆複測（多人 4 筆需 3 台真機；GPS 用 ?gpsdebug=1 看 abs 標記）
2. ⏳ 道具給分：既有頁面 DB 已寫入 10 分者需編輯器手動改 0
3. 📋 下批：websocket.ts（1245）/ Home.tsx（1135）拆分；依賴 breaking 升級評估
