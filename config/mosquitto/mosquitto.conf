# 開發用 Mosquitto 設定（ADR-0024）
#
# ⚠️ 僅供本機開發／測試使用，允許匿名連線。
# 生產一律使用「託管 broker」＋ per-device 帳密 ＋ TLS(8883)，不可套用本檔。

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type error
log_type warning
log_type notice
