# 批 4 — 建議新增 2 項 — 2026-05-12

> #10 GPS 方向箭頭 + #11 整場 BGM 音量
> #9 道具獎勵分數欄位 — 業主需求解讀有歧義（item-level points vs 元件 rewardPoints + rewardItems）、留下次評估

## #10 GPS 地圖方向箭頭
- `GpsMissionMap.tsx`：玩家位置 marker 加箭頭 SVG、用 `bearingDegrees()` 旋轉指向目標
- 玩家走動時、藍點上方箭頭隨地理方位自動轉

## #11 遊戲基本設定 整場 BGM + 音量
- `games` schema 加 `bgmVolume` int default 50
- migration: `2026-05-12-game-bgm-volume.sql`
- 遊戲基本設定 admin UI：
  - 「整場背景音樂」區塊
  - 音檔 URL input + 上傳按鈕（accept=audio/*）
  - audio preview（已上傳即可試聽）
  - 音量 slider 0-100（step 5）
- `useBgmPlayer` API 加 `setNormalVolume(0-100)`：
  - 內部用 `normalVolumeRef`、不靠 hardcoded 0.5
  - duck / unduck / toggleMute 都用 ref 值
- `GamePlay.tsx` useEffect 監聽 `game.bgmVolume` 變動、套用到 BGM player

## 驗收
- tsc 0 / smoke 51/51
