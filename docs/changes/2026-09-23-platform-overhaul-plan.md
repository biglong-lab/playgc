# CHITO 通盤規劃：經銷合作 + 管理優化 + 介面動線 + 指引 / 一頁一功能 / 防呆 — 2026-09-23

> 狀態：🟢 業主已確認（2026-09-23）— 照規劃從 P0 開始；P0 實作中
> 來源：業主 2026-09-23「針對系統做通盤的盤點……幫系統做一個完整的通盤規劃……/plan」
> 盤點：後台介面動線、平台商業 / 經銷、權限 / 引導 / 防呆 三路並行盤點 → planner 整合

---

## 背景（業主原話）

> 針對系統做通盤的盤點，在介面設定可以更友善，針對業務推展，有公司來談合作，針對業務推展，經銷、管理設定，
> 幫系統做一個完整的通盤規劃，讓我們的服務可以有業務經銷合作、優化整體管理、介面動線、設定更完整，
> 依照需求搭配指引、一頁一功能，來防呆，以及更方便的使用

## 盤點結論（改前）

### 經銷 / 商業
- 單層多租戶：場域是最上層，**沒有 partner / organization 任何結構**；跨場域只有 super_admin（全有或全無）
- 已有：/apply 申請 → 平台審核自動建場 + 試用；4 方案（free/pro/enterprise/revshare）；抽成計費邏輯；平台後台 27 頁；
  代理商 Public API v1；業務工具 /pitch /roi /showcase /find-scenario /template-market /faq + 訪客 demo
- 缺口：平台角色形同虛設（platform_admins 從未寫入、requirePlatformRole 沒路由用）；計費無閉環（無排程、無收款、
  停權 / 試用過期不擋登入、「我的方案」升級按鈕 disabled）；分潤無表（代理商帳務靠描述文字人工算）；
  /apply 無經銷代碼；定價兩套不一致（Pricing.tsx vs DB 方案）；審核建場沒建預設角色 / 申請人帳號

### 權限 / 模組 / 防呆
- 權限顆粒太粗：game:edit 80 次、game:view 63 次，預約 / POS / 營收都掛這兩個；5 個程式用了但沒建的權限鍵
- 🐛 `useGamePermissions.ts:72-75` 漏認 field_director（新場域預設管理員被判不能編輯）
- 模組關掉只藏選單，API / 排程照跑（不符「積木可拆卸」）；3 處重複模組開關 UI
- 防呆不一致：原生 confirm() 20 檔、prompt() 19 處；軟刪除只有 POS；未存提醒只 1 頁；發佈前檢查只在前端、列表頁可繞過、後端不驗

### 介面動線
- 側欄 46 項、92 條路由、約 30 頁無入口；三套導航（側欄 / 麵包屑 / ⌘K）不同步；孤兒頁 8+
- 同一件事分散：場域設定 7 頁、模組開關 3 處、定價 4 處、遊戲基本資料 3 處；FieldSettingsPage 2343 行、AdminDashboard 1606 行
- 建遊戲 6 個入口、從零到可玩約 10 步跨 6 頁；編輯器手機不能用

### 🔴 已查證的營運 bug
1. POS 折扣後找零用折扣前金額（`PosCheckout.tsx:220`、確認鈕 `:486`）→ 收銀員會少找錢
2. 編輯器未存離開會丟內容（`game-editor/index.tsx:423,435,478-492`；預覽讀伺服器舊資料 `GamePreview.tsx:70`）
3. 建立流程壞連結：匯入示範導到不存在的 /edit（`AdminGames.tsx:69`）、精靈「立即測試」開錯路由（`GameWizard.tsx:154`）、精靈「發布遊戲」只關視窗（`StepComplete.tsx:74-87`）
4. 發佈無把關（`GamesTable.tsx:352` 繞過、`admin-games.ts:421-444` 不驗 status、0 頁遊戲可發佈）
5. 場域管理員能關掉「所有場域」的元件（`AdminFeatureFlags.tsx:60`、`admin-feature-flags.ts:111-130`）
6. POS 首頁現場人員死按鈕（`PosDashboard.tsx:108-110`）；兩個不同動作都叫「今日結帳」
7. 重產 QR 無確認、取消預約可空白原因、刪除支出不進垃圾桶、「新增場域」對場域管理員顯示但會被拒
8. 系統設定欄位存了無作用（`AdminSettings.tsx:291-370`）；斷線寬限期要重啟才生效

## 驗收目標

| 業主的話 | 驗收標準 |
|---|---|
| 業務經銷合作 | 經銷用代碼 / 專屬連結帶客戶申請；平台一鍵審核完成建場 + 方案 + 預設角色 + 客戶帳號 + 經銷授權；經銷登入看得到自己的客戶場域與分潤對帳 |
| 優化管理 | 能建「只做 POS」「只看報表」角色；模組關掉時選單、API、排程同時停 |
| 介面動線 | 側欄 / 麵包屑 / ⌘K 同源；孤兒頁 = 0；建遊戲 1 個入口、從零到可玩 ≤5 步 |
| 設定更完整 | 場域設定收成一個「設定中心」；「存了無作用」的欄位 = 0 |
| 依需求搭配指引 | 依已開模組產生完成度清單，進度存伺服器 |
| 一頁一功能 | 每頁 1 個主要動作；單檔 ≤800 行 |
| 防呆 | 原生 confirm / prompt = 0；危險操作三級規格；發佈前後端都把關；未存離開會被攔 |

## 設計原則（日後驗收用）

1. **一頁一功能**：一頁最多 1 顆主按鈕、標題「動詞＋名詞」；超過 3 個主題拆子頁
2. **設定四要素**：說明、預設值、驗證（前後端共用 zod）、儲存回饋；每個設定鍵都要有讀取點（守護測試）
3. **危險操作三級**：L1 可復原（垃圾桶 + 復原提示）／L2 影響他人（確認框寫影響範圍）／L3 不可逆或全場域（輸入名稱確認 + 原因 ≥5 字 + 稽核）
4. **看不到就打不到**：選單、按鈕、API 判斷同源；死按鈕 = 0
5. **導航單一來源**：由 `admin-menu.ts` 生成側欄、麵包屑、⌘K
6. **指引＝完成度清單**：每一步直接連到要做事的頁；空狀態附「下一步」
7. **積木可拆卸**：模組關掉 → 選單、API、cron 同時停
8. **未存不丟**：攔截站內換頁；預覽讀草稿

## 目標資訊架構

| 角色 | 入口 | 群組 |
|---|---|---|
| 平台 | /platform | 總覽／場域與申請／經銷夥伴（新）／方案與帳務／系統／稽核（27 頁收成 6 群） |
| 經銷 | /partner（新） | 總覽／客戶場域／名單／推廣工具／分潤對帳 |
| 場域管理員 | /admin | 首頁（完成度 + 今日）／遊戲／營運／會員與獎勵／數據與營收／排解／設定中心 |
| 現場人員 | /pos | 4 顆主鈕：掃描、收款、今日預約、券核銷 + 交班 |

新舊頁對照：/admin/fields → 設定中心 › 場域資料；/admin/field-settings（2343 行）拆「品牌外觀／場域頁內容／遊戲預設與場次／AI」；
/admin/settings 無作用欄位撤下（DB 不動）；三處模組開關 → 設定中心 › 模組（由 module-registry 生成）；
/admin/feature-flags 全域開關移平台、場域端只寫本場域 override；LINE / Telegram / Email → 設定中心 › 通知與整合（Telegram 改場域級）；
我的方案 → 設定中心 › 方案與用量；角色 / 帳號 → 設定中心 › 人員與權限；四處定價 → 營收 › 價格總表（唯讀彙整）；
兩個「今日結帳」→「交班鎖帳」／「推送日報」。

## 經銷合作模型（對照 hung-blocks L0–L4）

- L0 partners 積木程式碼（所有經銷共用）／L1 每家經銷可開通的模組／L2 品牌、專屬連結、預設方案、分潤比例（資料）／
  L3 經銷只看得到授權場域／L4 客製邏輯走 webhook 訂閱事件；白牌延後
- 四件套：`server/modules/partners/{index.ts, server/}`、`shared/schema/partners.ts`、`client/src/pages/partner/`；守護：外部禁止 import 內部檔
- 新表（全部只加不刪）：partners、partner_members、partner_fields（解除寫 detached_at 不刪列）、partner_leads、partner_commissions（platform_transaction_id 唯一 → 冪等）；
  field_applications 加可空 `partner_code`
- 權限：不新增 systemRole；經銷在客戶場域建一筆 admin_account，套「經銷報表／經銷協助／代管」三種預設角色；
  放寬 switch-field：同一 firebaseUserId 在目標場域有 active 帳號即可切（多館業主也受惠）
- 開通：`/p/:slug` 品牌落地頁 → `/apply?ref=CODE` → 平台審核呼叫共用 `provisionField()`（合併兩套建場流程、補建預設角色與申請人帳號）→
  事件 `field.provisioned` → partners 寫授權、lead 標 won → Resend 開通信
- 分潤：來源 platform_transactions 已付款的訂閱與抽成；月結產 partner_commissions（冪等）；平台核准後人工匯款標 paid；平台沒收到錢不產分潤
- ADR：0026 經銷夥伴模型／0027 權限鍵拆分與模組開關後端化／0028 導航單一來源與設定中心／計費閉環另寫（對照 ADR-0006）

## 分階段計畫（每階段可單獨部署；部署一律等業主口令）

| 階段 | 內容 | 驗收（Playwright 真實流程） | 天數 |
|---|---|---|---|
| P0 營運 bug | POS 找零；編輯器未存攔截 + 預覽讀草稿；三個壞連結；發佈前後端共用把關 + status 白名單；POS 死按鈕與「今日結帳」改名；重產 QR 確認、取消預約原因、支出垃圾桶、新增場域只給 super_admin；元件開關限本場域；補認 field_director；斷線寬限期熱讀 | 收 1000 折 200 付 1000 找 200；編輯中離開被攔；0 頁遊戲發佈 400；field_director 能編輯；A 場域關元件不影響 B | 3–4 |
| P1 收尾進行中 | 部署已完成批次（身份規則、參賽者檢查、POS 區間）；競賽個人制接線 + 前端；接力 + 編輯器分段 | 兩人建賽→開賽→分數→時間到自動結算→排名；接力 3 人自動交棒 | 6–8 |
| P2 底座 | 補 5 個缺的權限鍵 + 新增 pos:operate / pos:manage / booking:manage / revenue:view / report:view（先只加遷移再換檢查）；module-registry + requireModule + cron 跳過；enablePos / enableBooking 回填；方案 features 執行時檢查；provisionField()；Telegram 場域級；輕量事件匯流排；守護棘輪 | 「只做 POS」角色只看到 4 顆鈕、打遊戲 API 403；關對戰模組 API 403；審核通過申請人能直接登入 | 6–8 |
| P3 經銷 MVP | 四張 partner 表；平台「經銷夥伴」頁；/p/:slug；ref 歸因；審核自動授權；放寬 switch-field；FieldSelector「我的場域」 | 平台建經銷 A → A 的連結申請 → 審核 → A 只看得到新場域報表 → 場域主可解除授權 | 5–6 |
| P4 資訊架構 / 設定中心 / 防呆 | 導航單一來源、依角色選單；拆 FieldSettingsPage、AdminDashboard；DangerAction / ReasonDialog / SettingField 共用元件；換掉 confirm / prompt；孤兒頁掛入口或下架入口；編輯器可收合 + 手機斷點 | 守護：confirm/prompt=0、孤兒頁=0、三套導航差異=0；手機 390px 能編輯 | 10–14 |
| P5 指引與建遊戲動線 | 完成度清單存伺服器；單一入口：選情境→模板→編輯→發佈檢查→QR；四套模板 UI 合成一個目錄 | 新場域主照清單走，到玩家掃 QR 玩完 ≤5 步 | 6–8 |
| P6 經銷後台與分潤 | /partner 全頁；月結分潤；經銷品牌套落地頁與 demo | 經銷只看到自己的場域；月結重跑不重複 | 5–7 |
| P7 計費閉環 | 月帳單排程；停權 / 試用過期先警示後擋；TapPay；「我的方案」升級；競賽隊伍制；API key 存 DB | 依業主決策再定 | 8–12 |

總估時 49–67 工作天（單人約 2.5–3.5 個月）；P0–P3 完成（約 20–26 天）即可對外談經銷。

## 風險與取捨

| 等級 | 風險 | 處理 |
|---|---|---|
| HIGH | 改權限鍵回歸（game:edit 80 次、game:view 63 次） | 先只加的資料遷移再換檢查；守護測試加反例 |
| HIGH | 拆大檔 | 先補 e2e 再拆；純搬移與改行為分開 commit |
| HIGH | 停權誤鎖正式客戶 | 先一個部署週期只警示不擋 |
| MEDIUM | 放寬 switch-field 擴大攻擊面 | 限同一 firebaseUserId + 帳號 active；稽核；反例測試 |
| MEDIUM | 模組後端化誤關現有功能 | 依「現在在用就開」回填 |
| MEDIUM | 單人維運負擔 | 不引入新基礎設施；分潤先人工付款；每階段可單獨部署 |
| LOW | 現有資料相容 | 全新表或可空欄位 |

## 需業主決定

| 事項 | 選項 | 建議 |
|---|---|---|
| 分潤模式 | A 一次性介紹費／B 按訂閱與抽成持續抽 %／C 批發價、經銷自訂售價 | B（每家可設比例），C 延後 |
| 經銷能否自己建場 | 不行（帶客戶申請、平台審）／有上限可建／完全自主 | 先不行，信任後逐家開 |
| 經銷進客戶場域權限 | 報表／協助／代管，由客戶同意 | 預設報表，客戶可調高 |
| 定價統一 | 以 DB 4 方案為準／以 Pricing.tsx 為準 | 以 DB 為準，公開頁改讀 API；委辦列服務項目 |
| 先經銷還是先管理 | 先經銷／先管理／交錯 | 交錯：P2 → P3 → P4 |
| 金流與停權 | 經銷上線前／之後 | 之後（P7），先人工帳單 |
| 競賽隊伍制 | P1 一起／P7 | P7（除非已有客戶要） |
| 白牌 | 延後／納入 | 延後 |

## 業主決定（2026-09-23）

| 事項 | 決定 |
|---|---|
| 開工範圍 | 照規劃從 P0 開始，依序推進；每階段完成回報、部署等業主口令 |
| 分潤模式 | **持續抽成 %**（依客戶訂閱費與交易抽成，每家經銷比例可設） |
| 經銷能否自己建場 | **可以，但有上限**（partners.can_create_fields + max_fields，逐家設定） |
| 定價統一 | **以系統 4 方案為準**（公開定價頁改讀系統設定；委辦列為服務項目） |
| 其餘 | 依建議：經銷進客戶場域預設「報表」權限、交錯推進 P2→P3→P4、金流 / 停權放 P7、競賽隊伍制 P7、白牌延後 |

## 實作步驟

### P0 營運 bug（2026-09-23 完成，未部署）

| commit | 內容 |
|---|---|
| `e8d1d426` | POS 折扣後找零算錯（找零 / 剛好 / 大金額確認 / 按鈕金額改用折扣後應收）**— 已部署生產並驗證** |
| `34c65a75` | 編輯器未存攔截（返回 / AI 產生器 / 資源連結先問「儲存後離開 / 不存離開 / 取消」）+ 標記已實測不再洗掉編輯內容 |
| `9e143bd1` | 預覽讀到剛編輯的內容（有未存變更先存、預覽不吃舊快取） |
| `71c58521` `461cc864` `4b90d71b` | 三個壞連結：匯入示範後導向、立即測試改開管理員預覽、「發布遊戲」真的呼叫發布 API |
| `4faca141` `78ad6d6b` `f3fd8a22` | 發佈把關：頁面驗證搬 shared 前後端共用、`/publish` 加 status 白名單 + 內容檢查、列表發布前先檢查並列出問題 |
| `34c65a75`（補完） | 編輯器 / 遊戲設定頁改成已發佈也跑同一套檢查（原本那兩條路沒把關）；編輯器改「先存內容再改狀態」 |
| `d54d31bc` | 補認 field_director，新場域管理員可編輯遊戲 |
| `7c41bc77` `eb66a597` | 元件開關限本場域（碰全域 / 別場域 403）＋ 前端確認影響範圍；順手修好原本新增一律 500 |
| `21139e25` | POS 首頁依權限只顯示點得進去的按鈕 |
| `d62b4398` | 兩個「今日結帳」改名：櫃檯＝交班鎖帳、報表＝推送日報 |
| `c2ba7898` | 重新產生 QR 前先確認（寫明已印出的會失效） |
| `187c382a` | 後台取消預約原因必填 ≥5 字 + 確認步驟（前後端同規則） |
| `4673d62c` | 刪除的現金支出進垃圾桶、可還原（已交班鎖帳當日還原回 409） |
| `e0664e23` | 「新增場域」只對平台管理員顯示（與後端同一份規則） |
| `ed94a887` | 斷線寬限期改為斷線時讀場域設定（30 秒快取），不必重啟；原本設定存了沒作用 |

### P1 競賽 / 接力（2026-09-23 完成，未部署）

| commit | 內容 |
|---|---|
| `7d29a71a` | 伺服器端：大廳資料層、伺服器控制倒數 / 開賽 / 結算 + 5 秒巡檢、分數自場次進度同步、接力自動交棒、未驗證入口改 410、WS 房間限參賽者 |
| `3167b64d` | 玩家動線：賽事 ID 進網址、邀請碼 / 分享連結加入、開賽自動進遊戲、遊戲中賽事看板、接力等待畫面與只玩自己那段、結算頁 |
| `12cab72d` | 遊戲設定可選競賽 / 接力並設定規則（限時、倒數、人數、接力分段含平均分配與即時檢查）；接力沒分段不能發佈 |
| `3a7ea509` | e2e：兩位訪客、兩支手機，競賽與接力完整流程 |

### 部署後安全審查修正（2026-09-23）

| commit | 內容 |
|---|---|
| `e4f4280f` | **HIGH**：建立場次改白名單（原本可自帶 score / status）；完成場次一律驗分數（原本沒帶 score 就整段跳過）；進度分數同步走同一套驗證；接力非當棒不收分 / 不觸發交棒；接力沒分段時人數上限失效；現金支出還原改要 `pos_cash_admin`；公開排名不再露信箱前綴 — **已部署 `96147b3f`** |
| `cb86af23` | `PATCH /api/games/:id` 禁改 fieldId / creatorId / publicSlug / isDemo；元件開關 platform_admin 併平台級 + 不再寫出 fieldId=null 死資料；寬限期快取上限真的生效 |
| `49958e83` | 邀請碼不再出現在公開列表（只給房主 / 參賽者）＋ 私人房 ＋ 房主踢人；賽事讀取與建賽加 per-IP 限流；巡檢批次上限 + 過期房間自動取消；接力分數只收自己那段頁面；草稿遊戲不能開賽 |
| `10dc409e` | 大廳配合私人房 / 踢人；修結算數字停在中途（結束後停止輪詢 + 舊快取） |

### P2 底座（2026-09-23 完成，未部署）

| commit | 內容 |
|---|---|
| `855e7d75` | **權限鍵目錄**：31 鍵單一來源（shared/lib/permission-catalog），新增 pos:operate / pos:manage / booking:manage / revenue:view / report:view / user:manage；啟動冪等補齊 + 依對照表補發給既有角色（本機實測補 6 鍵、補發 52 筆）；守護測試鎖住「程式用到的鍵一定要在目錄裡」。**路由檢查改用細鍵留待下一批**（規劃：先只加遷移再換檢查） |
| `82f523b6` | **模組登錄表**：8 模組宣告 API 前綴 / 選單 / 排程 / 依賴；moduleGuard 掛 /api 之下，關掉 → 403 module_disabled；預約排程依場域跳過；後台 GET/PATCH /api/admin/fields/:id/modules（含稽核）。開關沿用既有 enableBattleArena / enableCompetitiveMode，不會把已關掉的功能又打開（等同「現在在用就開」的回填） |
| `61018161` | **方案功能執行時生效**：12 個功能鍵目錄；模組守門一併判斷方案（免費版開了水彈模組也用不了）；外部 API 需 api_access；自帶 AI 金鑰需 ai_key_byo、品牌外觀需 custom_brand；白牌 / 自訂網域 / 優先支援標記為尚未接程式 |
| `c1642983` | **provisionField()**：開通收成一條路（場域 → 預設角色 → 訂閱 → 申請人帳號 → 發事件）。修掉「平台審核通過但申請人登不進去」（沒角色、沒帳號）。**輕量事件匯流排**：記憶體內、訂閱者錯誤隔離 |
| `6b5e693e` | **Telegram 場域級**：場域可設自己的群組，沒設沿用環境變數；預約 / 晨報 / 遊戲開玩通知都帶場域 |
| `65f864e7` | **守護棘輪**：模組宣告要對得上真實路由、場域設定欄位不得「存了沒人讀」、事件命名規則、原生 confirm/prompt 鎖在 7 處只減不增 |

P2 驗收對照：
- 「只做 POS」角色：權限鍵已拆好可建角色；**打遊戲 API 403 要等下一批把路由檢查換成細鍵**
- 「關對戰模組 API 403」：✅ 已可（moduleGuard + 後台 API）；模組開關 UI 留 P4 設定中心
- 「審核通過申請人能直接登入」：✅ 已建場域管理員帳號（email 綁定 Firebase 登入）

## 驗證

| 項目 | 方式 | 結果 |
|---|---|---|
| POS 折扣後找零 | 生產實機（1000 折 200 付 1000） | ✅ 找零 200；生產 bundle 已確認含新算法 |
| 競賽完整流程 | Playwright 真瀏覽器 ×2 手機（訪客） | ✅ 建賽 → 分享連結加入 → 開賽 → 各自玩 → 全員完成自動結算；同分比完成速度；DB 狀態正確 |
| 接力完整流程 | Playwright 真瀏覽器 ×2 手機（訪客） | ✅ 第 1 棒只玩 1–2 頁 → 自動交棒 → 第 2 棒玩 3–4 頁 → 自動結算、全隊總分 |
| 既有玩家動線沒壞 | 既有 e2e（訪客 QR / 組隊） | ✅ 4 支全過 |
| 型別 / 單元 / 整合 | `npx tsc --noEmit`、vitest 全套 | ✅ 0 型別錯誤；3667 項通過 |
| P0 後台頁面（編輯器攔截、發佈把關 UI、元件開關、POS 權限按鈕、預約原因、支出垃圾桶） | 單元 / 整合測試已覆蓋；**真瀏覽器驗收需要後台登入帳號** | ⏳ 待業主實機點一次（或提供可用於本機 e2e 的後台帳號） |
| 安全審查修正 | vitest 全套 3678 項 + 競賽 / 接力 / 組隊 e2e 真瀏覽器 | ✅ 全過（型別 0 錯誤） |
| P2 底座 | vitest 全套 3739 項（新增守護與服務測試 40+）＋ 競賽 / 接力 / 訪客 QR e2e 真瀏覽器回歸 | ✅ 全過（型別 0 錯誤）；本機啟動實測權限同步冪等、模組守門不誤擋 |
